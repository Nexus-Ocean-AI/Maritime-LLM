# Core dependencies
pymongo[srv]
transformers>=4.37.0
accelerate>=0.20.0
peft>=0.4.0
datasets>=2.14.0
torch>=2.1.0
scipy
protobuf
sentencepiece

# Training optimization
bitsandbytes>=0.41.0
trl>=0.7.0
xformers

# Unsloth for 2-5x speedup (requires CUDA)
# Install with: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# unsloth @ git+https://github.com/unslothai/unsloth.git

# Optional: Flash Attention 2 for long context optimization (CUDA only)
# Install separately with: pip install flash-attn --no-build-isolation
# flash-attn>=2.0.0

# Monitoring
tensorboard
tqdm
